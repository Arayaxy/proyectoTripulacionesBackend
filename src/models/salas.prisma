model Sala {
  id_sala            String    @id @default(uuid(7)) @db.Uuid
  nombre_Sala        String
  tipo_sala          String
  capacidad_max_sala String
  nota_sala          String
  eventos            Evento[]
  espacios           Espacio[]

  @@map("salas")
}
